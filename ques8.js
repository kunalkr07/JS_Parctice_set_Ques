//Create a function that will convert from Celsius to Fahrenheit
const prompt=require("prompt-sync")();
        let fehrenheight;
let celcius=parseInt(prompt("Enter temparature"))
    fehrenheight=(celcius*1.8)+32;
    console.log(`The ferenheight is ${fehrenheight}`);
